import React, { Component } from 'react';

import App from '../../App';

class Login extends Component {
  render() {
    return (
        <App title="Login"/>
    );
  }
}

export default Login;
